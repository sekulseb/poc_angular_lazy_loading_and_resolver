import { Component } from '@angular/core';

@Component({
  selector: 'app',
  templateUrl: './app.html',
  styleUrls: ['./app.css']
})
export class App {
  title = 'POC Angular Lazy Loading Modules with Data Resolver (feature-b)';
}
